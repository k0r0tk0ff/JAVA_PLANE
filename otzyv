Плюсы 
+ для сборки проекта используется Maven;
+ проект разрабатывался с использованием системы контроля версий Git;
+ настройки подключения к БД вынесены в файл свойств;
+ литералы вынесены в константы;
+ работа программы логируется;
+ для очистки таблицы используется TRUNCATE;
+ правильная пакетная запись в таблицу БД;
+ потоковое формирование xml;
+ потоковое чтение xml.
Минусы:
- оверинжинеринг проекта, ненужное переусложнение структуры;
- закрывается Scanner scanner = new Scanner(System.in), что приводит к закрытию System.in;
- в ConsoleInput странная проверка (maxValue == 0 || maxValue < 0), не знает о существовании <=  ?
- неуместное использование оберточного типа результата метода public Integer getMaxValue(), значение null не имеет смысла в логике программы;
- объявление конструктора класса private PropertiesFileConfiguration(Properties properties) throws IOException  зачем то содержит throws IOException,
хотя реально никогда не возбуждает его;
- при загрузке из таблицы БД читаются сразу все данные, запись результата выполняется в коллекцию, что при большом N может привести к переполнению оперативной памяти;
- лишний код в RawXmlSequenceWriter, зачем то заполняется данными StringBuilder.
